spring:
  datasource:
    url: jdbc:h2:file:./data/escapelog;MODE=MYSQL;DATABASE_TO_UPPER=false;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  h2:
    console:
      enabled: true      # 🔥 콘솔 켜기
      path: /h2-console  # 🔥 접속 경로
